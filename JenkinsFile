pipeline {
    agent any

    stages {
        stage('Actually building'){
        	steps{
        		sh 'pwd'
        		sh 'ls -alrt'
        		sh 'chmod 777 build.sh'
        		sh './build.sh'
        	}       	
        }
    }
}
