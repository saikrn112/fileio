pipeline {
    agent any

    stages {
        stage('Actually building'){
        	steps{
        		sh 'pwd'
        		sh 'ls -alrt'
        		sh './build.sh'
        	}
        	
        }

    }
}
